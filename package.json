{
  "main": "src/main.ts",
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^24.10.1",
    "rimraf": "^6.0.1",
    "typescript": "^5.9.2"
  },
  "scripts": {
    "build": "npm run clean && npx tsc",
    "clean": "rimraf dist",
    "prod": "node --no-deprecation dist/main.js --prod",
    "start": "npm run build && node --no-deprecation dist/main.js"
  },
  "dependencies": {
    "@tensorflow-models/knn-classifier": "^1.2.6",
    "@tensorflow/tfjs-node": "^4.22.0",
    "canvas": "^3.2.0",
    "express": "^5.2.1"
  }
}
