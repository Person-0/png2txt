# Samples
This directory should contain labelled images that will be used to train the KNN classifier.<br>
Add simple captcha puzzle images with their names as the text which is present on them.<br> eg. `ABCDE.jpg` which is a captcha puzzle with text `ACBDE` in it.<br> More images equals better accuracy.